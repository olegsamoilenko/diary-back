# Project Memory

This file is long-term project memory for Codex work on the backend repository. Read it before backend-focused answers, preferably from newest entries first.

## Core Rules
- Do not edit project files unless the user explicitly writes the Ukrainian phrase "Внеси зміни".
- Exception: `.codex/project-memory.md` may be updated without a separate "Внеси зміни" when new significant information appears about the product, architecture, UX decisions, collaboration rules, or important project agreements.
- The workspace contains two related projects: `diary-front` and `diary-back`. Use this file for backend-focused work; use `diary-front/.codex/project-memory.md` for frontend-focused work.
- In the shared `diary-front` + `diary-back` workspace, read both project memory files from newest entries before answering, then focus on the relevant repo.

## Recent Decisions
- 2026-06-18: Diary entry prompt positive-event test worked very well on "old friend wrote after two years". Desired behavior confirmed: for pleasant life events, the model should not reduce the reflection to generic happiness/praise; it should find the living inner meaning. In this case it identified reconnection not only with a friend but with a lighter/spontaneous/open part of the user, warned against trying to recreate the past, and reframed the upcoming meeting as discovering what can live now. This is a good quality bar for positive diary entries.
- 2026-06-18: Diary entry dialog prompt quality test on the "want to be important / black swan / dirty" thread was strong. The dialog correctly moved from a specific relationship trigger to a broader self-worth theme, separated state from identity ("I feel bad" vs "I am bad"), recognized shame language without diagnosing, avoided empty reassurance, used gentle humor sparingly, and asked precise next-step questions. This is a desired quality bar for vulnerable diary-entry dialogs: deepen carefully, name the mechanism, avoid motivational flattening, and keep the user in control of how far to go.
- 2026-06-18: Diary entry prompt test on a relationship/importance scenario worked well: the model identified the mechanism where a small rejection-like event turns into an internal test of "am I important?", with shame around vulnerability and silence/coldness as protection. User noted this also maps to a broader "being important to the world" theme, not only close relationships. This is a useful target pattern: the model should surface the deeper need and mechanism without reducing it to "do not be offended".
- 2026-06-18: First diary entry reflection should include a warm Nemory welcome. This is expected behavior for the first entry, but the welcome must not overpower the actual analysis; for normal later entries, the model should go straight into the useful diary-entry reflection.
- 2026-06-18: Implemented diary entry Short/Full closing takeaway rule to mirror check-ins: `shortText` must not include "phrase of the day", slogans, or a final takeaway line. In `fullText`, if a concise final anchor is genuinely useful, it should be renamed away from "phrase of the day" and use the same style as check-ins, preferably `Ключова думка:`, summarizing the central insight without becoming a motivational slogan.
- 2026-06-18: Diary entry AI prompt was aligned with the check-in prompt approach by replacement, not layering. Old generic entry reflection instructions were removed. Entry reflections now use a diary-specific method: surface meaning -> deeper reading -> central issue -> mechanism -> practical resolution, adapted for free-form diary records rather than structured check-ins. Entry Short/Full output now mirrors the check-in consistency contract: fullText is the source useful reflection, shortText is a compact cut of the same ideas, and both must add value instead of retelling the entry. Ordinary entry dialogs also now use a diary-entry dialog method adapted from check-in dialogs.
- 2026-06-18: Check-in dialog prompt refined. `checkin_dialog` should be a focused conversation about the structured check-in and earlier reflection, not a new reflection and not short/full JSON. It should answer the user's current message directly, use check-in mood/metrics/reflection/dialog history only when relevant, keep the same practical insight standard as check-in reflection, avoid filler, use stable neutral Nemory voice, and stay within about 2000 characters. Backend `getMaxOutTokens('checkin_dialog')` is capped separately from ordinary dialogs.
- 2026-06-18: Nemory voice rule for check-in AI reflections: avoid random grammatical gender shifts such as Ukrainian "я б думала"/"я б думав". Nemory should speak in a stable neutral voice and prefer neutral constructions ("варто", "можна", "корисно буде", "тут допоможе") instead of gendered first-person conditional/past forms.
- 2026-06-17: Testing strategy changed again for the near term: automated Maestro E2E is paused because stabilizing/writing it is too slow. The user will manually pass E2E/product flows, while Codex should focus automated coverage on broad frontend/backend unit and integration tests across the app. Backend fixture/prod-like E2E ideas may be revisited later, but current implementation work should prioritize unit/integration coverage for real business logic and edge cases.
- 2026-06-16: E2E testing strategy direction changed. Keep frontend/backend unit tests broad and frequent, but rebuild E2E as prod-like flows using real app startup, real backend/database state, anonymous user creation, real entry save/load, AI responses/dialogs where practical, and backend fixture/setup endpoints for deterministic scenarios. Mock only external/unstable integrations such as Google Play/IAP or similar services. Backend will likely need test-only fixture endpoints guarded from production to reset/create test users, assign plan states, seed/cleanup entries, and prepare plan/AI edge cases.
- 2026-06-16: Frontend subscription/AI tests should mock backend subscription responses and Google/`react-native-iap`; do not drive real Play Billing in Maestro regression. The user manually tests real subscriptions on a test Google account. Backend IAP verification (`/iap/create-sub`, Google `subscriptionsv2.get`, RTDN `/iap/pub-sub`) should be covered later by separate backend tests.
- 2026-06-16: Create Entry AI/subscription test planning now includes stale frontend plan state. Backend `src/ai/guards/plan.guard.ts` is the source of truth for AI access: it can reject via HTTP or websocket `plan_error` when the actual plan is missing/inactive/canceled after expiry/expired/pending/on hold/paused/refunded/trial expired/credit exceeded. These errors should cause the frontend to refresh/drop active plan status and show the relevant plan modal while keeping create-entry open if the user continues without plan.
- 2026-06-16: New Codex chat continues the previous React Native + Nest Nemory work after the prior chat stalled. Current collaboration rule remains strict: without the exact phrase "Внеси зміни", do not edit project files except `diary-front/.codex/project-memory.md` and `diary-back/.codex/project-memory.md`; significant product/architecture/UX/testing/collaboration decisions should still be recorded in memory without separate edit permission.
- 2026-06-14: User clarified the collaboration rule in Ukrainian: without the exact phrase "Внеси зміни", do not edit any files except `diary-front/.codex/project-memory.md` and `diary-back/.codex/project-memory.md`. Before answering, read both project memory files from the newest entries and account for them. Update these memory files without a separate edit-permission phrase when new significant product, architecture, UX, interaction-rule, or agreement information appears. Keep memory compact by grouping older notes if it grows too large.
- 2026-06-13: Added a separate AI content mode for structured check-in dialogs: `AiContentMode` is now `entry | dialog | checkin | checkin_dialog`. The existing `stream_ai_dialog` websocket event remains the transport for dialogs, but may receive `mode: "checkin_dialog"`; `AiGateway` defaults all other values to ordinary `dialog` for backward compatibility. `AiService.generateComment` now has a check-in-dialog-specific system prompt, treats `checkin_dialog` token usage as `TokenType.DIALOG`, uses dialog output limits, and `ai-preferences.prompt.ts` treats `checkin_dialog` like `dialog` for style rules such as omitting phrase-of-the-day.
- 2026-06-13: Model update/pricing decision: do not switch diary AI to `gpt-5.5` yet because it is significantly more expensive for the diary use case. Keep current Claude lineup for now. Backend credit pricing for `gpt-5.4` must be corrected to official OpenAI pricing: $2.50 input / $15 output per 1M tokens, so `MODEL_PRICE_CREDITS[AiModel.GPT_5_4]` should be `25000/150000`; any legacy per-token helper such as `COAST_TOKEN` should also handle `GPT_5_4` explicitly with `2.5/15.0`.
- 2026-06-13: Backend AI prompt flow currently lives mostly in `src/ai/ai.service.ts`, while websocket streaming entrypoints live in `src/ai/gateway/ai.gateway.ts`. `AiGateway` exposes `stream_ai_comment` and `stream_ai_dialog`, both delegating to `AiService.generateComment(...)`; `generateComment` currently switches between entry and dialog prompts via `isDialog`. For check-ins, the intended direction is to add a distinct check-in prompt/context mode instead of sending structured check-ins as ordinary journal entries. Check-in mode should preserve template/question/answer structure, mood, metrics, similar context, memories, goals, and time context.

- 2026-06-17: Backend AI websocket streams now guard client disconnect during entry comment/dialog generation. `AiGateway` stops comment/dialog streaming callbacks when the socket is disconnected, skips emitting done/error in that cancellation case, and lets the frontend stop button avoid continuing backend-side generation work as far as the gateway can interrupt the stream loop. Backend typecheck passes.
- 2026-06-17: Planned AI reflection contract change: entry AI comment should support Short reflection and Full reflection. Short is about 100-150 words and shown by default; Full is available on request/show-full. Backend prompt/stream contract will likely need to return both short and full content reliably, with the short content streamed first for UX. Need avoid fragile plain-text separators if possible; prefer a structured protocol or explicit websocket events.
- 2026-06-17: AI reflection compatibility requirement clarified: keep existing full AI answer field/contract compatible for current users; add an optional short reflection field instead of renaming existing content. Backend should return/store both short and full for new entry comments, while old comments without short content must continue to render full text.
- 2026-06-17: Frontend now sends `generateShortReflection: true` in the `stream_ai_comment` websocket payload for entry AI comments. Backend follow-up should read this boolean to choose the prompt/response contract for Short + Full entry reflections; dialog streams should stay unchanged.
- 2026-06-17: Backend gateway now accepts `generateShortReflection?: boolean` on `stream_ai_comment` and passes it as the final optional argument to `AiService.generateComment`; `generateComment` defaults it to `false` so check-in/dialog call sites are unchanged. For now the value is only available/logged for entry generation; prompt/output parsing/storage are still pending.
- 2026-06-17: Backend Short/Full entry reflection prompt contract implemented. When `stream_ai_comment` sends `generateShortReflection=true`, `AiService.generateComment` asks the model for a strict JSON object with `shortText`, `fullText`, and `tags`, uses a non-stream chat completion for the entry comment, parses the object, records token usage/credits, and `AiGateway` emits structured `ai_stream_comment_done` with both texts. Legacy entry comments without the flag and all dialog/check-in streams keep the old chunk streaming behavior.
- 2026-06-17: Short/Full prompt meaning clarified: `fullText` is the source reflection, and `shortText` is a compressed version of that same reflection, preserving the same core idea/tone/practical direction while omitting detail. `shortText` should use `\n\n` paragraph breaks only when it naturally has separate semantic blocks (for example emotion, meaning/pattern, next step); otherwise it remains one paragraph.
- 2026-06-17: Backend check-in AI analysis now supports the same Short/Full reflection contract as entries. `stream_ai_checkin` accepts `generateShortReflection`, passes it to `AiService.generateComment`, and emits structured `ai_stream_checkin_done` with `content`, `fullText`, `shortText`, and `tags` when available. Check-in Short/Full uses a dedicated prompt block that preserves template/question/answer structure and keeps check-in responses shorter than diary reflections. Legacy check-ins without the flag still stream text chunks normally; check-in/dialog streams also now guard client disconnect like entry/dialog streams.
- 2026-06-18: Fixed the Short/Full parser edge case for low-content check-ins. The check-in prompt may intentionally return JSON with `shortText` set and `fullText` empty; `AiService.parseShortFullReflection` must treat that as valid, use `shortText` as the normalized `content/fullText`, and only fallback to raw text when both `shortText` and `fullText` are missing. This prevents raw JSON from being emitted as ordinary AI text.
- 2026-06-18: AI reflection prompt quality issue identified from a real structured check-in: current Short/Full output is too much paraphrase/summary of the user's answers and not enough useful added value. Next prompt revision should be shorter and stricter: avoid retelling the entry/check-in, extract one real pattern or tension, give one concrete next step/experiment, and keep check-in reflections compact. Short and Full should differ by depth, not by duplicating the same summary.
- 2026-06-18: Prompt direction clarified further: do not narrow every AI reflection to exactly one action, because check-ins/entries can contain several meaningful themes. The target voice is non-AI-ish and practically useful: no retelling, no generic praise, no "analysis article"; instead add new insight, outside perspective, non-obvious but grounded signals, practical usefulness, and 1-3 relevant threads when the content supports them. Start with structured check-in prompts first; regular diary entry prompts will be refined later. Current GPT-5.4 should be sufficient if the prompt is stricter and less bloated.
- 2026-06-18: First structured check-in prompt refinement implemented in `src/ai/ai.service.ts`. The check-in system prompt now frames the task as "useful perspective from this snapshot" instead of summary, prioritizes usefulness over length, allows 1-3 meaningful threads when supported, discourages AI-ish summaries/motivational quotes/psychology articles, and treats practical directions as optional/natural rather than exactly one forced action. The check-in Short/Full output block now makes word ranges soft defaults, forbids padding to length, requires fullText to add real value beyond shortText, and explicitly blocks retelling/generic validation. Regular diary entry prompt is unchanged for now.
- 2026-06-18: Short/Full check-in contract clarified and implemented: `fullText` is the source useful reflection, and `shortText` must be a compact cut of the main ideas from `fullText`, like a news lead to an article. They must not contain different interpretations or separate main thoughts; fullText should expand/support exactly what shortText promised, adding depth/context/nuance/practical detail without changing the theme. The prompt now includes an explicit consistency check before returning JSON.
- 2026-06-18: Further check-in prompt quality insight: model still produces "correct but low-value" reflections that praise/validate instead of helping with the underlying problem. Prompt should teach the model to infer actionable problem signals from avoidance/relief phrasing: "did not fall into chaos" implies a chaos/slipping pattern; "had enough strength not to pressure myself" implies a usual self-pressure pattern; "do not fuel morning anxiety" implies morning anxiety around tasks. Useful reflection should unpack the underlying mechanism and offer concrete techniques/approaches for that problem, not merely say the user did well. This is not over-invention when grounded in the user's own wording.
- 2026-06-18: Check-in prompt direction corrected away from enumerating situation types. The prompt should not grow into a list of cases like "if achievement, then baseline", "if pain point, then..." etc. It now asks the model to read beneath the surface without predefined categories/checklists, find the most useful non-obvious signal if one exists, and use the chaos example only as an example of depth, not a reusable rule. The desired output is a thoughtful diary companion insight: "I had not fully noticed that, but it is true and useful", not "yes, that is what I wrote".
- 2026-06-18: Next check-in prompt rewrite should be framed positively around how to analyze and respond, not primarily around prohibitions. Main prompt should define the model's analytic method: identify the central issue, separate main vs secondary signals, name the user's current baseline/problem plainly when evidence is strong, move from insight to practical restructuring of the situation, and format the answer around useful analysis. "Do not" rules should be a small final guardrail block for obviously unwanted behaviors (retelling, empty praise, generic AI talk), not the dominant prompt.
- 2026-06-18: User clarified the desired check-in prompt method in detail. The model should first understand the user's own surface meaning, then analyze the text from several angles to find what is hidden between the lines, then explicitly name the real problem/situation when evidence is strong, then explain why it may be happening, and finally give concrete practical steps. Example: if a user says they managed not to fall into chaos, the useful insight is not praise; it may be that chaos has become the user's default baseline and the task is to build a structured day, not keep fighting chaos. Practical advice should be specific, such as planning the next day the evening before, separating primary and secondary goals, estimating capacity/time, moving excess tasks, and starting the morning with a clear plan.
- 2026-06-18: Check-in prompt tuning continued after model output still stayed too soft and narrow. New target: when evidence is strong, the model should state the central issue directly instead of hedging/paraphrasing, and should not reduce a broad/systemic pattern to one tiny action. Full check-in reflection should open the theme as: central problem -> mechanism -> changed operating principle -> concrete system/steps. For the chaos example, a weak answer is "choose one task tomorrow"; a useful answer names that chaos has become the default mode and proposes a system-level correction: evening planning, capacity estimation, priority separation, morning entry point, secondary-task boundaries, and a definition of "enough".
- 2026-06-18: Check-in prompt quality improved on a real `decision` template test. Desired successful behavior: model should identify that the core issue may not be "is this opportunity good?" but "am I deciding from fear of missing out rather than real capacity?". Useful answer frames the decision around conditions and boundaries: clarify weekly hours/deadlines/responsibility, check what must be removed from the schedule, distinguish "yes", "yes with limits", and "no, not now", and expose whether the user's "yes" comes from interest/resource or anxiety. User said this is close to real analysis and is starting to feel good.
- 2026-06-18: Conflict check-in prompt lesson: a merely acceptable answer says "set boundaries" and gives wording templates. A stronger answer should distinguish a one-off conflict from a broken/repeating system, emotion from the role the user is taking, help from silently taking over someone else's responsibility, and teamwork from self-sacrifice that keeps a bad process functioning. For team/interpersonal check-ins, the model should name roles like buffer/rescuer/emergency fallback when grounded, explain why the pattern repeats while that role remains unchanged, and propose process-level correction: separate help from ownership, define responsibility boundaries, move recurring problems into explicit agreements, and make the hidden cost visible.
- 2026-06-18: Check-in Short/Full formatting refinement: "phrase of the day" should not be included by default; only use a closing phrase when it naturally strengthens the reflection and does not cheapen the topic. Prefer paragraph flow over numbered lists. If a list is genuinely clearer, it must be short and numbering must be correct/sequential.
- 2026-06-18: Check-in terminology refinement requested: do not call any closing takeaway in check-ins "phrase of the day", because structured check-ins are not necessarily day summaries. If a check-in response needs a final memorable line, treat it as a check-in-specific takeaway/anchor rather than "phrase of the day", and avoid it in shortText unless it is genuinely natural.
- 2026-06-18: Check-in closing takeaway naming decision: remove closing takeaway lines entirely from `shortText`. In `fullText`, if a concise final anchor is genuinely useful, label it `Ключова думка:`. It must summarize the central insight of that specific check-in and must not read like a motivational slogan or day summary.
