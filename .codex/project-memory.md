# Project Memory

This file is long-term project memory for Codex work on the backend repository. Read it before backend-focused answers, preferably from newest entries first.

## Core Rules
- Do not edit project files unless the user explicitly writes the Ukrainian phrase "Внеси зміни".
- Exception: `.codex/project-memory.md` may be updated without a separate "Внеси зміни" when new significant information appears about the product, architecture, UX decisions, collaboration rules, or important project agreements.
- The workspace contains two related projects: `diary-front` and `diary-back`. Use this file for backend-focused work; use `diary-front/.codex/project-memory.md` for frontend-focused work.
- In the shared `diary-front` + `diary-back` workspace, read both project memory files from newest entries before answering, then focus on the relevant repo.

## Recent Decisions
- 2026-06-18: Diary entry prompt positive-event test worked very well on "old friend wrote after two years". Desired behavior confirmed: for pleasant life events, the model should not reduce the reflection to generic happiness/praise; it should find the living inner meaning. In this case it identified reconnection not only with a friend but with a lighter/spontaneous/open part of the user, warned against trying to recreate the past, and reframed the upcoming meeting as discovering what can live now. This is a good quality bar for positive diary entries.
- 2026-06-18: Diary entry dialog prompt quality test on the "want to be important / black swan / dirty" thread was strong. The dialog correctly moved from a specific relationship trigger to a broader self-worth theme, separated state from identity ("I feel bad" vs "I am bad"), recognized shame language without diagnosing, avoided empty reassurance, used gentle humor sparingly, and asked precise next-step questions. This is a desired quality bar for vulnerable diary-entry dialogs: deepen carefully, name the mechanism, avoid motivational flattening, and keep the user in control of how far to go.
- 2026-06-18: Diary entry prompt test on a relationship/importance scenario worked well: the model identified the mechanism where a small rejection-like event turns into an internal test of "am I important?", with shame around vulnerability and silence/coldness as protection. User noted this also maps to a broader "being important to the world" theme, not only close relationships. This is a useful target pattern: the model should surface the deeper need and mechanism without reducing it to "do not be offended".
- 2026-06-18: First diary entry reflection should include a warm Nemory welcome. This is expected behavior for the first entry, but the welcome must not overpower the actual analysis; for normal later entries, the model should go straight into the useful diary-entry reflection.
- 2026-06-18: Implemented diary entry Short/Full closing takeaway rule to mirror check-ins: `shortText` must not include "phrase of the day", slogans, or a final takeaway line. In `fullText`, if a concise final anchor is genuinely useful, it should be renamed away from "phrase of the day" and use the same style as check-ins, preferably `Ключова думка:`, summarizing the central insight without becoming a motivational slogan.
- 2026-06-18: Diary entry AI prompt was aligned with the check-in prompt approach by replacement, not layering. Old generic entry reflection instructions were removed. Entry reflections now use a diary-specific method: surface meaning -> deeper reading -> central issue -> mechanism -> practical resolution, adapted for free-form diary records rather than structured check-ins. Entry Short/Full output now mirrors the check-in consistency contract: fullText is the source useful reflection, shortText is a compact cut of the same ideas, and both must add value instead of retelling the entry. Ordinary entry dialogs also now use a diary-entry dialog method adapted from check-in dialogs.
- 2026-06-18: Check-in dialog prompt refined. `checkin_dialog` should be a focused conversation about the structured check-in and earlier reflection, not a new reflection and not short/full JSON. It should answer the user's current message directly, use check-in mood/metrics/reflection/dialog history only when relevant, keep the same practical insight standard as check-in reflection, avoid filler, use stable neutral Nemory voice, and stay within about 2000 characters. Backend `getMaxOutTokens('checkin_dialog')` is capped separately from ordinary dialogs.
- 2026-06-18: Nemory voice rule for check-in AI reflections: avoid random grammatical gender shifts such as Ukrainian "я б думала"/"я б думав". Nemory should speak in a stable neutral voice and prefer neutral constructions ("варто", "можна", "корисно буде", "тут допоможе") instead of gendered first-person conditional/past forms.
- 2026-06-17: Testing strategy changed again for the near term: automated Maestro E2E is paused because stabilizing/writing it is too slow. The user will manually pass E2E/product flows, while Codex should focus automated coverage on broad frontend/backend unit and integration tests across the app. Backend fixture/prod-like E2E ideas may be revisited later, but current implementation work should prioritize unit/integration coverage for real business logic and edge cases.
- 2026-06-16: E2E testing strategy direction changed. Keep frontend/backend unit tests broad and frequent, but rebuild E2E as prod-like flows using real app startup, real backend/database state, anonymous user creation, real entry save/load, AI responses/dialogs where practical, and backend fixture/setup endpoints for deterministic scenarios. Mock only external/unstable integrations such as Google Play/IAP or similar services. Backend will likely need test-only fixture endpoints guarded from production to reset/create test users, assign plan states, seed/cleanup entries, and prepare plan/AI edge cases.
- 2026-06-16: Frontend subscription/AI tests should mock backend subscription responses and Google/`react-native-iap`; do not drive real Play Billing in Maestro regression. The user manually tests real subscriptions on a test Google account. Backend IAP verification (`/iap/create-sub`, Google `subscriptionsv2.get`, RTDN `/iap/pub-sub`) should be covered later by separate backend tests.
- 2026-06-16: Create Entry AI/subscription test planning now includes stale frontend plan state. Backend `src/ai/guards/plan.guard.ts` is the source of truth for AI access: it can reject via HTTP or websocket `plan_error` when the actual plan is missing/inactive/canceled after expiry/expired/pending/on hold/paused/refunded/trial expired/credit exceeded. These errors should cause the frontend to refresh/drop active plan status and show the relevant plan modal while keeping create-entry open if the user continues without plan.
- 2026-06-16: New Codex chat continues the previous React Native + Nest Nemory work after the prior chat stalled. Current collaboration rule remains strict: without the exact phrase "Внеси зміни", do not edit project files except `diary-front/.codex/project-memory.md` and `diary-back/.codex/project-memory.md`; significant product/architecture/UX/testing/collaboration decisions should still be recorded in memory without separate edit permission.
- 2026-06-14: User clarified the collaboration rule in Ukrainian: without the exact phrase "Внеси зміни", do not edit any files except `diary-front/.codex/project-memory.md` and `diary-back/.codex/project-memory.md`. Before answering, read both project memory files from the newest entries and account for them. Update these memory files without a separate edit-permission phrase when new significant product, architecture, UX, interaction-rule, or agreement information appears. Keep memory compact by grouping older notes if it grows too large.
- 2026-06-13: Added a separate AI content mode for structured check-in dialogs: `AiContentMode` is now `entry | dialog | checkin | checkin_dialog`. The existing `stream_ai_dialog` websocket event remains the transport for dialogs, but may receive `mode: "checkin_dialog"`; `AiGateway` defaults all other values to ordinary `dialog` for backward compatibility. `AiService.generateComment` now has a check-in-dialog-specific system prompt, treats `checkin_dialog` token usage as `TokenType.DIALOG`, uses dialog output limits, and `ai-preferences.prompt.ts` treats `checkin_dialog` like `dialog` for style rules such as omitting phrase-of-the-day.
- 2026-06-13: Model update/pricing decision: do not switch diary AI to `gpt-5.5` yet because it is significantly more expensive for the diary use case. Keep current Claude lineup for now. Backend credit pricing for `gpt-5.4` must be corrected to official OpenAI pricing: $2.50 input / $15 output per 1M tokens, so `MODEL_PRICE_CREDITS[AiModel.GPT_5_4]` should be `25000/150000`; any legacy per-token helper such as `COAST_TOKEN` should also handle `GPT_5_4` explicitly with `2.5/15.0`.
- 2026-06-13: Backend AI prompt flow currently lives mostly in `src/ai/ai.service.ts`, while websocket streaming entrypoints live in `src/ai/gateway/ai.gateway.ts`. `AiGateway` exposes `stream_ai_comment` and `stream_ai_dialog`, both delegating to `AiService.generateComment(...)`; `generateComment` currently switches between entry and dialog prompts via `isDialog`. For check-ins, the intended direction is to add a distinct check-in prompt/context mode instead of sending structured check-ins as ordinary journal entries. Check-in mode should preserve template/question/answer structure, mood, metrics, similar context, memories, goals, and time context.

- 2026-06-17: Backend AI websocket streams now guard client disconnect during entry comment/dialog generation. `AiGateway` stops comment/dialog streaming callbacks when the socket is disconnected, skips emitting done/error in that cancellation case, and lets the frontend stop button avoid continuing backend-side generation work as far as the gateway can interrupt the stream loop. Backend typecheck passes.
- 2026-06-17: Planned AI reflection contract change: entry AI comment should support Short reflection and Full reflection. Short is about 100-150 words and shown by default; Full is available on request/show-full. Backend prompt/stream contract will likely need to return both short and full content reliably, with the short content streamed first for UX. Need avoid fragile plain-text separators if possible; prefer a structured protocol or explicit websocket events.
- 2026-06-17: AI reflection compatibility requirement clarified: keep existing full AI answer field/contract compatible for current users; add an optional short reflection field instead of renaming existing content. Backend should return/store both short and full for new entry comments, while old comments without short content must continue to render full text.
- 2026-06-17: Frontend now sends `generateShortReflection: true` in the `stream_ai_comment` websocket payload for entry AI comments. Backend follow-up should read this boolean to choose the prompt/response contract for Short + Full entry reflections; dialog streams should stay unchanged.
- 2026-06-17: Backend gateway now accepts `generateShortReflection?: boolean` on `stream_ai_comment` and passes it as the final optional argument to `AiService.generateComment`; `generateComment` defaults it to `false` so check-in/dialog call sites are unchanged. For now the value is only available/logged for entry generation; prompt/output parsing/storage are still pending.
- 2026-06-17: Backend Short/Full entry reflection prompt contract implemented. When `stream_ai_comment` sends `generateShortReflection=true`, `AiService.generateComment` asks the model for a strict JSON object with `shortText`, `fullText`, and `tags`, uses a non-stream chat completion for the entry comment, parses the object, records token usage/credits, and `AiGateway` emits structured `ai_stream_comment_done` with both texts. Legacy entry comments without the flag and all dialog/check-in streams keep the old chunk streaming behavior.
- 2026-06-17: Short/Full prompt meaning clarified: `fullText` is the source reflection, and `shortText` is a compressed version of that same reflection, preserving the same core idea/tone/practical direction while omitting detail. `shortText` should use `\n\n` paragraph breaks only when it naturally has separate semantic blocks (for example emotion, meaning/pattern, next step); otherwise it remains one paragraph.
- 2026-06-17: Backend check-in AI analysis now supports the same Short/Full reflection contract as entries. `stream_ai_checkin` accepts `generateShortReflection`, passes it to `AiService.generateComment`, and emits structured `ai_stream_checkin_done` with `content`, `fullText`, `shortText`, and `tags` when available. Check-in Short/Full uses a dedicated prompt block that preserves template/question/answer structure and keeps check-in responses shorter than diary reflections. Legacy check-ins without the flag still stream text chunks normally; check-in/dialog streams also now guard client disconnect like entry/dialog streams.
- 2026-06-18: Fixed the Short/Full parser edge case for low-content check-ins. The check-in prompt may intentionally return JSON with `shortText` set and `fullText` empty; `AiService.parseShortFullReflection` must treat that as valid, use `shortText` as the normalized `content/fullText`, and only fallback to raw text when both `shortText` and `fullText` are missing. This prevents raw JSON from being emitted as ordinary AI text.
- 2026-06-18: AI reflection prompt quality issue identified from a real structured check-in: current Short/Full output is too much paraphrase/summary of the user's answers and not enough useful added value. Next prompt revision should be shorter and stricter: avoid retelling the entry/check-in, extract one real pattern or tension, give one concrete next step/experiment, and keep check-in reflections compact. Short and Full should differ by depth, not by duplicating the same summary.
- 2026-06-18: Prompt direction clarified further: do not narrow every AI reflection to exactly one action, because check-ins/entries can contain several meaningful themes. The target voice is non-AI-ish and practically useful: no retelling, no generic praise, no "analysis article"; instead add new insight, outside perspective, non-obvious but grounded signals, practical usefulness, and 1-3 relevant threads when the content supports them. Start with structured check-in prompts first; regular diary entry prompts will be refined later. Current GPT-5.4 should be sufficient if the prompt is stricter and less bloated.
- 2026-06-18: First structured check-in prompt refinement implemented in `src/ai/ai.service.ts`. The check-in system prompt now frames the task as "useful perspective from this snapshot" instead of summary, prioritizes usefulness over length, allows 1-3 meaningful threads when supported, discourages AI-ish summaries/motivational quotes/psychology articles, and treats practical directions as optional/natural rather than exactly one forced action. The check-in Short/Full output block now makes word ranges soft defaults, forbids padding to length, requires fullText to add real value beyond shortText, and explicitly blocks retelling/generic validation. Regular diary entry prompt is unchanged for now.
- 2026-06-18: Short/Full check-in contract clarified and implemented: `fullText` is the source useful reflection, and `shortText` must be a compact cut of the main ideas from `fullText`, like a news lead to an article. They must not contain different interpretations or separate main thoughts; fullText should expand/support exactly what shortText promised, adding depth/context/nuance/practical detail without changing the theme. The prompt now includes an explicit consistency check before returning JSON.
- 2026-06-18: Further check-in prompt quality insight: model still produces "correct but low-value" reflections that praise/validate instead of helping with the underlying problem. Prompt should teach the model to infer actionable problem signals from avoidance/relief phrasing: "did not fall into chaos" implies a chaos/slipping pattern; "had enough strength not to pressure myself" implies a usual self-pressure pattern; "do not fuel morning anxiety" implies morning anxiety around tasks. Useful reflection should unpack the underlying mechanism and offer concrete techniques/approaches for that problem, not merely say the user did well. This is not over-invention when grounded in the user's own wording.
- 2026-06-18: Check-in prompt direction corrected away from enumerating situation types. The prompt should not grow into a list of cases like "if achievement, then baseline", "if pain point, then..." etc. It now asks the model to read beneath the surface without predefined categories/checklists, find the most useful non-obvious signal if one exists, and use the chaos example only as an example of depth, not a reusable rule. The desired output is a thoughtful diary companion insight: "I had not fully noticed that, but it is true and useful", not "yes, that is what I wrote".
- 2026-06-18: Next check-in prompt rewrite should be framed positively around how to analyze and respond, not primarily around prohibitions. Main prompt should define the model's analytic method: identify the central issue, separate main vs secondary signals, name the user's current baseline/problem plainly when evidence is strong, move from insight to practical restructuring of the situation, and format the answer around useful analysis. "Do not" rules should be a small final guardrail block for obviously unwanted behaviors (retelling, empty praise, generic AI talk), not the dominant prompt.
- 2026-06-18: User clarified the desired check-in prompt method in detail. The model should first understand the user's own surface meaning, then analyze the text from several angles to find what is hidden between the lines, then explicitly name the real problem/situation when evidence is strong, then explain why it may be happening, and finally give concrete practical steps. Example: if a user says they managed not to fall into chaos, the useful insight is not praise; it may be that chaos has become the user's default baseline and the task is to build a structured day, not keep fighting chaos. Practical advice should be specific, such as planning the next day the evening before, separating primary and secondary goals, estimating capacity/time, moving excess tasks, and starting the morning with a clear plan.
- 2026-06-18: Check-in prompt tuning continued after model output still stayed too soft and narrow. New target: when evidence is strong, the model should state the central issue directly instead of hedging/paraphrasing, and should not reduce a broad/systemic pattern to one tiny action. Full check-in reflection should open the theme as: central problem -> mechanism -> changed operating principle -> concrete system/steps. For the chaos example, a weak answer is "choose one task tomorrow"; a useful answer names that chaos has become the default mode and proposes a system-level correction: evening planning, capacity estimation, priority separation, morning entry point, secondary-task boundaries, and a definition of "enough".
- 2026-06-18: Check-in prompt quality improved on a real `decision` template test. Desired successful behavior: model should identify that the core issue may not be "is this opportunity good?" but "am I deciding from fear of missing out rather than real capacity?". Useful answer frames the decision around conditions and boundaries: clarify weekly hours/deadlines/responsibility, check what must be removed from the schedule, distinguish "yes", "yes with limits", and "no, not now", and expose whether the user's "yes" comes from interest/resource or anxiety. User said this is close to real analysis and is starting to feel good.
- 2026-06-18: Conflict check-in prompt lesson: a merely acceptable answer says "set boundaries" and gives wording templates. A stronger answer should distinguish a one-off conflict from a broken/repeating system, emotion from the role the user is taking, help from silently taking over someone else's responsibility, and teamwork from self-sacrifice that keeps a bad process functioning. For team/interpersonal check-ins, the model should name roles like buffer/rescuer/emergency fallback when grounded, explain why the pattern repeats while that role remains unchanged, and propose process-level correction: separate help from ownership, define responsibility boundaries, move recurring problems into explicit agreements, and make the hidden cost visible.
- 2026-06-18: Check-in Short/Full formatting refinement: "phrase of the day" should not be included by default; only use a closing phrase when it naturally strengthens the reflection and does not cheapen the topic. Prefer paragraph flow over numbered lists. If a list is genuinely clearer, it must be short and numbering must be correct/sequential.
- 2026-06-18: Check-in terminology refinement requested: do not call any closing takeaway in check-ins "phrase of the day", because structured check-ins are not necessarily day summaries. If a check-in response needs a final memorable line, treat it as a check-in-specific takeaway/anchor rather than "phrase of the day", and avoid it in shortText unless it is genuinely natural.
- 2026-06-18: Check-in closing takeaway naming decision: remove closing takeaway lines entirely from `shortText`. In `fullText`, if a concise final anchor is genuinely useful, label it `Ключова думка:`. It must summarize the central insight of that specific check-in and must not read like a motivational slogan or day summary.
- 2026-06-21: Manual QA bug-fix workflow agreed for the shared frontend/backend work. The user will report hands-on testing issues one at a time. For each confirmed issue, the expected path is: diagnose and fix the bug, add/update automated unit or integration coverage where practical, then update the frontend `tests/TEST_E2E_COVERAGE_MAP.md` so the scenario is covered in future manual release checks.
- 2026-06-21: Diary statistics expanded beyond entries/dialogs to check-ins. Backend now has event tables `checkins_stats` and `checkin_dialogs_stats`, cumulative tables `total_checkins_stats` and `total_checkin_dialogs_stats`, controller endpoints `add-checkin-stat` / `add-checkin-dialog-stat`, daily cron collection, and user activity counters `checkins` / `checkin_dialogs`. Check-in stats store a stable English `checkin_name` sent by the frontend, not localized names or technical entry kinds.
- 2026-06-22: Current shared app work phase is performance/speed optimization. Immediate task is frontend-only lint inspection for unused variables/imports and unnecessary comments; do not edit application files unless the user explicitly says "Внеси зміни".
- 2026-06-22: Frontend performance cleanup workflow is now file-by-file: analyze one screen/component for unused code and performance opportunities, then after explicit edit permission optimize it, run focused tests plus frontend typecheck, and continue into child components.
- 2026-06-22: Frontend optimization notes in `diary-front/.codex/optimization.md` should be written in Ukrainian and include three parts per file/component: changes made, automatic checks, and manual tests.
- 2026-06-22: Frontend optimization notes in `diary-front/.codex/optimization.md` are append-only by optimization batch: new optimization work should add a new dated batch instead of rewriting an older one.
- 2026-06-22: Frontend Calendar optimization lesson: focus refresh should update entry data without remounting entry cards, because remounting reloads image backgrounds and visibly flickers. Entry card preview hydration should keep `entry.previewHtml` visible until hydrated HTML is ready.
- 2026-06-22: Frontend optimization pass rule уточнено для спільного контексту: у файлах фронта, які зараз оптимізуються, також виносити inline styles у `StyleSheet` або мемоізувати style-масиви через `useMemo`, коли це зменшує зайві render-алокації; візуальний стиль при цьому не змінювати.
- 2026-06-22: Frontend Calendar UX decision for shared context: day cells in week and month calendar views should show immediate pressed feedback when tapped, so users can see that the tap registered even before/while selected-day data updates.
- 2026-06-22: Frontend Diary optimization started for shared context. First pass on `app/(tabs)/diary.tsx` removed a debug log and unused screen-level active-mood state, memoized/stabilized feed rows/date headers/footer loader/delete confirm flow, and added baseline `tests/unit/app/diaryScreen.test.js` coverage.
- 2026-06-22: Frontend EntryCard title visual regression fixed for shared context: `RichHtml` title mode now leaves room for Cyrillic descenders on the last line (`р`, `у`, `д`, `ц`) so title text is not clipped by the card surface/background.
- 2026-06-22: Frontend Diary `EntryCard` optimization continued for shared context. `DiaryFeedItemCard` remained a thin memo dispatcher; `EntryCard` now stabilizes gradient props, moves card/action/title/metrics inline styles into `StyleSheet`, removes the no-op expanded-style dependency, opens the action menu with fallback position before optional measurement, and clears delayed full-entry collapse timers. Frontend focused EntryCard/Diary/Calendar tests and typecheck pass.
- 2026-06-22: Frontend Diary `DailyCheckinCard` optimization completed for shared context. The card now stabilizes readable-gradient props, hitSlop/menu fallback/layout/edit/menu callbacks, uses absolute-fill overlay styles, centralizes image-background style branching, removes stale commented styles, and has lint-clean focused check-in tests. Frontend focused check-in/Diary/Calendar tests and typecheck pass.
- 2026-06-22: Frontend Diary `StructuredCheckinCard` optimization completed for shared context. The card now stabilizes readable-gradient props, hitSlop/menu fallback/layout/navigation/menu callbacks, memoizes the main pressable style callback, uses absolute-fill overlay styles, and centralizes image-background style branching. Frontend focused check-in/Diary/Calendar tests and typecheck pass.
- 2026-06-22: Frontend Diary `MetricsData` optimization completed for shared context. The top Diary metrics block is memoized, repeated metric value/tile/icon-slot pieces are memo components, inline root/icon/value styles moved into `StyleSheet`, and dynamic value/tile styles are memoized. Metric average/rounding/level/trend behavior stayed unchanged. Frontend focused MetricsData/Diary tests and typecheck pass.
- 2026-06-22: Frontend check-in card visual direction for shared context: next experiment should use one programmatic theme-aware "Check-in Pulse" background rather than separate image assets for every theme. It should derive palette from current frontend theme tokens so light, dark, and custom themes stay supported automatically, and visually signal check-ins as state/measurement/reflection rather than photo-like diary entries.
- 2026-06-22: Frontend Check-in Pulse experiment implemented for shared context. `EntryCardBackground` supports a check-in-only variant that draws a static programmatic theme-aware pulse background from current theme tokens; `DailyCheckinCard` and `StructuredCheckinCard` use it, while ordinary diary entry cards keep the previous background behavior. Frontend focused lint/Jest/typecheck pass.
- 2026-06-22: Frontend manual visual check for shared context: the first Check-in Pulse background is too subtle in light themes because card readability layers nearly hide the pulse/glow/rings. Next frontend adjustment should make the check-in motif visibly stronger while keeping text readability intact.
- 2026-06-22: Frontend Check-in Pulse visibility pass completed for shared context. Light-theme motif alpha, ring thickness, glow/sweep opacity, and bubble accents were strengthened; dark themes keep subtler alpha. The check-in background remains static/programmatic with no new assets or animation. Frontend focused lint/Jest/typecheck pass.

- 2026-06-22: Frontend manual visual check for shared context after Check-in Pulse visibility pass: dark theme is acceptable, but light theme motif is too attention-grabbing and should be softened so the pattern reads as background rather than competing with text.
- 2026-06-22: Frontend Check-in Pulse light-theme balance pass completed for shared context. Light themes now soften motif alpha/ring thickness/bubbles/glow/sweep and shift the large ring right so the pattern reads as background; dark-theme strength remains close to the approved look via theme-aware motif styles. Frontend focused lint/Jest/typecheck pass.

- 2026-06-22: Frontend check-in background next experiment for shared context: try a soft organic state grid first; fallback is a subdued wellness radar with partial arc/ticks/light radial scan if the grid does not work. Both should preserve text readability and remain background texture.
- 2026-06-22: Frontend check-in background experiment update for shared context: the soft state grid did not work visually because it was effectively invisible. The subdued wellness radar became visible after rendering above the check-in readability overlay but below card content; user considers it an acceptable fallback if no better check-in texture is found, but wants to continue exploring visual alternatives.
- 2026-06-22: Frontend check-in background candidate update for shared context: metric constellation (five subtle points plus thin connecting lines) looked "not bad" to the user and is now another possible fallback/candidate. Exploration continues with topographic wellbeing lines, breathing waves, and soft measuring ticks.
- 2026-06-22: Frontend check-in background candidates update for shared context: topographic wellbeing lines were tried and rejected; breathing waves were tried and rejected as not impressive. The active frontend candidate is soft measuring ticks, rendered above the check-in readability overlay but below content. Viable saved candidates so far are subdued wellness radar and metric constellation; measuring ticks still awaits user visual review.
- 2026-06-22: Frontend check-in background candidate update for shared context: soft measuring ticks were visually reviewed and rejected by the user. Viable saved frontend candidates remain subdued wellness radar and metric constellation; continue brainstorming new check-in textures beyond grid/radar/constellation/topography/waves/ticks.
- 2026-06-22: Frontend check-in background candidate update for shared context: soft state imprint was tried and rejected. The active frontend candidate is now paper texture + light veil: subtle theme-aware veil, thin paper-fiber lines, and tiny grain rendered above the check-in readability overlay but below content. The proposed micro-aura around emoji/metrics is skipped by user request.
- 2026-06-22: Frontend check-in background candidate update for shared context: paper texture + light veil was tried and rejected. The active frontend candidate is vertical light layers: broad translucent theme-aware vertical gradient layers rendered above the check-in readability overlay but below content. Micro-aura remains skipped by request.
- 2026-06-22: Frontend check-in background candidate update for shared context: vertical light layers were tried and rejected as not impressive. Soft capsule fields (overlapping translucent theme-aware capsule-like fields with a very light border, rendered above the readability overlay but below content) have "something in them" per user feedback and should be kept as a viable candidate, though not finalized yet.
- 2026-06-22: Frontend final check-in background exploration round started for shared context. Soft capsule fields remain a viable saved candidate. Active frontend candidate is glass refraction: thin theme-aware refracted light lines plus soft glow/edge-lines above the check-in readability overlay but below content. Metric mist is intentionally skipped by user request.
- 2026-06-22: Frontend check-in background final round update for shared context: active candidate changed from glass refraction to layered vellum. Layered vellum uses shifted translucent theme-aware sheet layers plus subtle edge-lines above the readability overlay but below content, aiming for a transparent film/material feel without explicit line-pattern decoration. Metric mist remains skipped.
- 2026-06-22: Frontend check-in background final round update for shared context: layered vellum and mood wash were both tried and rejected by the user. The active frontend candidate is soft scanner edge: a subtle right/bottom edge glow above the readability overlay but below content, keeping the main text area cleaner. Metric mist remains skipped.
- 2026-06-22: Frontend check-in background final round update for shared context: soft scanner edge was visually reviewed and rejected by the user. Remaining untried idea from the last approved list is calm diagonal veil; metric mist remains intentionally skipped. Viable saved candidates still include metric constellation and soft capsule fields, with subdued wellness radar as fallback.
- 2026-06-22: Frontend check-in background selection phase started after calm diagonal veil was also rejected. User asked to return to saved candidates one by one. Active frontend candidate is now metric constellation again; remaining saved candidates to revisit are soft capsule fields and subdued wellness radar fallback.
- 2026-06-22: End-of-day shared checkpoint for frontend work. User chose the first saved check-in background variant for now: metric constellation is the active check-in card background. Soft capsule fields remains an alternate saved candidate and subdued wellness radar remains fallback/history, but neither is active. User paused work for today and plans to continue frontend optimization tomorrow.
- 2026-06-23: Shared process note from frontend optimization: check-in background exploration/selection should not be tracked in `diary-front/.codex/optimization.md`; optimization log should contain actual optimization batches, checks, and manual tests, while background choices stay as product/design memory.
- 2026-06-23: Shared frontend checkpoint: create-entry optimization started. First batch optimized side-sheet/modal/metrics-row scaffolding (`AddNewEntrySideSheet`, `MetricsContainer`, `AddNewEntryModal`, `ClosePageModal`, `WarningAboutSubscriptionModal`) without touching rich editors or save-flow. Focused lint, focused Jest (`37/37`), and frontend typecheck passed; broad create-entry/edit-entry lint still has 69 warnings for later batches.
- 2026-06-23: Shared frontend UX note: create-entry metrics row now gives pressed feedback through `MetricsContainer` with a slight opacity/scale change and no layout shift. Focused frontend lint/Jest/typecheck passed.
- 2026-06-23: Shared frontend UX note: date trigger pressed feedback was aligned with metrics. In full-entry edit, date feedback is gated by edit mode (`canEditEntryState`), and metric-row pressed feedback only appears when an `onPress` handler exists, preventing read-only views from looking clickable. Focused frontend lint/Jest/typecheck passed.

- 2026-06-23: Shared frontend optimization checkpoint: create-entry metric picker files were cleaned and stabilized. The frontend removed unused metric picker imports/comments, moved repeated inline styles to `StyleSheet`, memoized handlers/selected styles/animated mood arrow nodes, and wrapped metric picker/modal/check components in `memo`. Focused frontend lint/Jest/typecheck passed; broad create/edit flow lint now has 49 warnings remaining.

- 2026-06-23: Shared frontend optimization checkpoint: `CreateEntryContent`/`CreateEntryTitle` were cleaned and stabilized in the create-entry flow. Unused props/imports/refs/comments/dead styles were removed, repeated inline dialog/content styles moved to `StyleSheet`, dynamic styles/callbacks memoized, and rich editor/AI stream behavior was left unchanged. Focused frontend lint/Jest/typecheck passed; create/edit flow lint now has 40 warnings remaining.

- 2026-06-23: Shared frontend optimization checkpoint: toolbar popup/style layer in create-entry was cleaned. `entry-content-style/*` unused imports/comments were removed, `ReachToolbarPopups` got stable memoized handlers/root style and a corrected style dependency list, focused frontend lint/Jest/typecheck passed, and create/edit flow lint now has 29 warnings remaining.
- 2026-06-23: Shared frontend regression fix: custom entry background image selection from gallery/camera can trigger native picker/cropper blur, so the background popup flow must participate in the same rich-editor toolbar focus lock/restore as font/color/size. Frontend now routes `onToolbarInteraction` through `ReachToolbarPopups -> BackgroundStyle -> SelectImageSourceModal` and allows `background-*` restore sources in create/edit entry flows. Manual E2E map includes the custom background picker no-collapse scenario.
- 2026-06-23: Shared frontend retest note: the custom background picker regression persists because Gallery/Camera immediately trigger native keyboard hide, and the frontend `useCollapseEditorUiOnBack` `keyboardDidHide` listener collapses editor UI when a popup/active editor exists. Next frontend fix should ignore/suppress `kbDidHide` collapse during native image-source actions, rather than relying only on post-picker focus restore.
- 2026-06-23: Shared frontend follow-up fix: custom background gallery/camera now uses an explicit native picker lock. The source modal raises `onNativePickerInteractionChange(true/false)` around picker/cropper/import; create/edit entry flows keep toolbar popup rendering alive while locked; `useCollapseEditorUiOnBack` suppresses only `keyboardDidHide` collapse during that lock. Focused frontend Jest/typecheck pass and app-code lint has no errors.
- 2026-06-23: Shared frontend rollback note: the explicit native picker lock was rolled back because manual testing showed a keyboard open/close loop after adding a custom background image. The frontend removed `onNativePickerInteractionChange`, `nativePickerInteractionRef`, `suppressKeyboardHideCollapseRef`, the extra hook test, and the `canShowPopups` lock changes. Future attempts should be smaller and rolled back immediately if they do not help.
- 2026-06-23: Shared frontend next direction for custom background picker: do not try to keep the keyboard open through native Gallery/Camera. Keep the background popup independent from keyboard hide because background applies to the active text rich editor. Source modal should close immediately after Gallery/Camera choice, keyboard may close, background popup should remain open, then after crop/import the frontend should programmatically refocus the text editor.
- 2026-06-23: Shared frontend implementation update: the smaller custom background picker fix now keeps only the background popup alive through `keyboardDidHide` when `showBg=true`; other toolbar popups still depend on keyboard state. The source image modal closes and waits one UI frame before launching Gallery/Camera, and the existing background toolbar interaction restore handles refocusing the active rich editor after crop/import. No native picker lock is reintroduced.
- 2026-06-23: Shared frontend retest note: the smaller custom background picker fix still needs correction. Adding a custom background image can cause keyboard double-open/twitch, likely from multiple `background-*` focus-restore events, and deleting a custom background image while the keyboard is open can show an empty-looking confirmation modal inside the background popup. Next frontend fix should keep the popup alive without a focus-restore storm and make the delete confirmation content/buttons visible in the nested popup portal.
- 2026-06-23: Shared frontend follow-up: custom background picker now avoids the keyboard refocus storm by emitting only one `background-*-done` event after successful image import, and create/edit entry flows restore the rich editor once after 180ms for background events. The custom background delete confirmation was also moved to a more robust `AbsoluteModal` title + bottomAction layout so the nested popup should no longer show an empty shell when the keyboard is open. Focused frontend tests/lint/typecheck pass.
- 2026-06-23: Shared frontend correction: the custom background delete confirmation still clipped because it was rendered in the local `background-style` portal inside the keyboard-constrained background popup. It now uses the global `overlay` host, so the confirm should render above the popup in full-screen coordinates while the source image picker modal remains local to `background-style`.
- 2026-06-23: Shared frontend correction update: the background delete confirmation design was restored to the old body layout; only its portal host stays global `overlay`. The background image source modal was also moved to global `overlay` with its existing design unchanged, so both modals should avoid keyboard/popup clipping without visual redesign.
- 2026-06-23: Shared frontend investigation note: custom entry background deletion currently soft-hides DB rows but leaves files under `documentDirectory/entry-backgrounds/`. Safe cleanup should be reference-aware because entry settings store background URIs. Image temp cache cleanup also appears miswired: `cleanupImageTempDirsOnColdStart()` exists for ImageManipulator/ImagePicker/crop-picker cache dirs, but root runtime effects call `restoreSubscriptions()` while logging the error as image-temp cleanup, so startup cleanup may not run.
- 2026-06-23: Shared frontend fix: cold-start temp cleanup for ImageManipulator/ImagePicker/crop-picker is now actually wired through root runtime effects. Hidden custom entry-background physical files are intentionally not deleted yet because existing entry settings can still reference their URIs; future cleanup must be reference-aware or migrate affected backgrounds first.
- 2026-06-23: Shared frontend UI fix: background popup source/delete modals now pass `avoidKeyboard={false}` to `AbsoluteModal`, so they remain centered in the global overlay even when the rich-editor keyboard is open. `AbsoluteModal` defaults to `avoidKeyboard=true`, preserving normal keyboard avoidance for other modals.
- 2026-06-23: Shared frontend custom background modal positioning lesson. The failed `avoidKeyboard` / `keyboardLiftEnabled` approach for `AbsoluteModal` did not affect first-open source/delete modal placement. The frontend now keeps background source/delete modals in the global `overlay` but passes `BackgroundStyle.keyboardHeight` as `keyboardInsetOverride`, so they center between the top of the screen and the active rich-editor keyboard. Future frontend modal fixes in this flow should use the known editor keyboard geometry instead of adding independent keyboard-state guesses inside the modal.
- 2026-06-23: Shared frontend optimization checkpoint: `TextReachEditor` pass completed. The content rich editor removed no-op listener/callback code, simplified duplicate height-offset branching, memoized heavy editor/RN style objects and picker/focus/helper callbacks, wrapped the component in `memo`, and removed unused `keyboardHeight` / `isFocusTextRichEditor` props from the content editor path to reduce unnecessary renders from keyboard-height updates. Focused frontend lint/Jest/typecheck pass; broad create/edit lint now has 23 warnings remaining, mainly in `TitleReachEditor`, `StreamingText`, `SelectConversationLanguageModal`, `EditEntryTitle`, `AddNewEntry`, and `ViewFullEntry`.
- 2026-06-23: Shared frontend optimization checkpoint: `TitleReachEditor` and `EditEntryTitle` pass completed. The title rich editor now stabilizes DOM helpers/undo-redo/style/focus handlers, memoizes heavy editor CSS/style objects, removes unused title editor props/dead refs, and is wrapped in `memo`; the edit-title wrapper removed stale imports/refs and stabilized simple callbacks. Focused frontend lint/Jest/typecheck pass; broad create/edit lint now has 12 warnings remaining, mainly in `AddNewEntry`, `ViewFullEntry`, `StreamingText`, and `SelectConversationLanguageModal`.
- 2026-06-23: Shared frontend optimization checkpoint: `StreamingText` pass completed. The stream typing component now keeps latest lifecycle callbacks in refs, stabilizes start/complete/stop/interval helpers, clears interval refs on cleanup/speed change, and is wrapped in `memo`; frontend regression tests cover partial stop text, latest callbacks, and speed changes. Focused/related frontend Jest, app lint, and typecheck pass; broad create/edit lint now has 10 warnings remaining in `AddNewEntry`, `ViewFullEntry`, and `SelectConversationLanguageModal`.
- 2026-06-23: Shared frontend optimization checkpoint: `SelectConversationLanguageModal` pass completed. The modal removed unused/default-import warnings, memoized schema/initial values, stabilized close/submit callbacks, moved spacing styles to `StyleSheet`, and is wrapped in `memo`; frontend focused lint/Jest/typecheck pass. Broad create/edit lint now has 8 warnings remaining, only in `AddNewEntry` and `ViewFullEntry`.
- 2026-06-23: Shared frontend optimization checkpoint: create/edit entry flow lint is now clean. `AddNewEntry` fixed props/settings/reset hook deps, stabilized side-sheet/content-ready/subscription callbacks, removed unnecessary toolbar-focus deps, and moved local inline styles into `StyleSheet`/memoized arrays. `ViewFullEntry` removed the matching unnecessary toolbar-focus deps and added `setContentReady` to its reset effect. Focused frontend create/edit Jest and typecheck pass; broad lint over create/edit entry files is `0 errors / 0 warnings`.
- 2026-06-23: Shared design direction: frontend light theme redesign is exploring a new default background/palette. User wants a mostly white/near-white first-launch theme with the primary color as a restrained accent. Abstract waves/light trails/floral/window concepts were rejected; current direction is recognizable diary/notebook-centered scenes that immediately communicate reflection/writing while preserving clean mobile UI space.
- 2026-06-23: Shared light-theme prompt lesson: the simple "first page" composition reads as another white surface with an open diary and is too repetitive. Next frontend theme concepts should make the diary/notebook scene meaningfully different in story and composition while staying mostly white with a restrained primary-color accent.
- 2026-06-23: Shared light-theme visual feedback: the "page in motion" diary prompt worked better because it gives the notebook a visible gesture/character. Keep exploring diary-centered concepts with meaningfully different stories; the page-motion direction is stronger than the plain open-notebook composition.
- 2026-06-23: Shared light-theme prompt lesson: the "diary and memory" concept with blank cards/polaroid-style paper failed to read as memory and instead looked like generic stationery. Future memory-themed prompts need clearer memory carriers such as faint overexposed instant photos, dated tabs, keepsake envelopes, or archive/time-capsule cues, kept subtle enough for a mostly white UI background.
- 2026-06-23: Shared frontend checkpoint: structured check-in flow optimization completed. The frontend stabilized the structured check-in create/details screens and state/mood/metric controls, added the standard readable title surface to structured check-in titles, and set one shared question placeholder (`Кілька слів про це...`) while keeping `Що ще важливо?` separate. Focused frontend lint/Jest/typecheck passed.
- 2026-06-23: Shared frontend checkpoint: daily check-in flow optimization completed. The frontend daily check-in modal now reuses shared check-in metric icon/color helpers, stabilizes metric handlers/render callbacks/submit/footer, and shared check-in metric UI components are memoized. `CreateCheckinProvider` remains isolated from UI helper imports while using typed daily drafts. Focused frontend lint/Jest/typecheck passed.
- 2026-06-23: Shared frontend checkpoint: Diary AI/chat layer optimization completed. Frontend stabilized/memoized `ChatInput`, `AiReflectionText`, and `ReviewAIModelAnswerModal`, removed dead chat input code/unused gap prop, and replaced duplicated review checkbox JSX with a reason-options list. Focused frontend lint/Jest/typecheck passed.
- 2026-06-23: Shared frontend checkpoint: Diary viewer layer optimization completed. Frontend memoized `EntryViewerPortal`, stabilized `ViewFullEntrySideSheet` open/close/timer/style handling, and cleaned `ViewReachEditor` by hoisting font CSS/editor styles and removing duplicate HTML updates. Focused frontend lint/Jest/typecheck passed.
- 2026-06-23: Shared frontend bugfix: Diary card action menu no longer lingers after tapping `Переглянути`. Frontend closes `EntryCardActionMenu` immediately and defers full-entry viewer open to the next animation frame to avoid React batching keeping the menu visible. Focused frontend lint/Jest/typecheck passed.
- 2026-06-23: Shared frontend checkpoint: Diary small primitives optimization completed. Frontend memoized/stabilized `EntryCardBackground`, `CheckinPulseBackground`, `EmojiBadge`, `Emoji`, `DateView`, and `DiaryEmptyState`, and cleaned `WritingEmptyState` unused imports/dead styles. Focused frontend lint/Jest/typecheck passed.
- 2026-06-23: Shared frontend checkpoint: Diary global modal optimization completed. Frontend cleaned/memoized trial welcome/ending and release/common notification modals, stabilized notification fetch/skip/mark callbacks, and added focused notification modal tests. Focused frontend lint/Jest/typecheck passed.
- 2026-06-23: Shared frontend handoff checkpoint before moving to a new chat. Frontend optimization started on the next Diary block: `components/diary/Subscription.tsx` and `components/diary/SubscriptionSideSheet.tsx`. The work is incomplete and should not be treated as verified. `Subscription.tsx` has partial optimization changes around memoization/stable handlers/styles/async storage guard. `SubscriptionSideSheet.tsx` has partial animation cleanup changes, but currently still needs completion: it references `styles.safeArea` without defining it, still has an old corrupted comment in `styles.sheet`, and no lint/Jest/typecheck has been run. The frontend `optimization.md` was not updated for this incomplete batch. Next frontend chat should finish or rollback these exact partial subscription changes first, then validate and log the completed batch.
- 2026-06-23: Shared process rule clarified: after code changes, Codex should update/add relevant tests and run them before reporting completion. Frontend subscription optimization checkpoint is now completed: `SubscriptionSideSheet` missing safe-area style/comment cleanup is fixed, new side-sheet coverage was added, focused frontend subscription Jest passed (`21/21`), focused lint passed, and frontend typecheck passed.
- 2026-06-23: Shared frontend subscription coverage update. The frontend added direct `Subscription.test.js` coverage for the global subscription wrapper and fixed `Subscription` storage flag loading to catch/log errors while preserving default flags, matching the settings `PlansSettings` behavior. Focused frontend subscription Jest passed (`29/29`), focused lint passed, and frontend typecheck passed.
- 2026-06-23: Shared frontend diary Support coverage update. The frontend added direct `tests/unit/components/diary/Support.test.js` coverage for the global support sheet, stabilized `components/diary/Support.tsx` with memoized schema/handlers and local forwarded-ref `sheetOpen`, and kept the existing settings Support contract aligned. Focused frontend Support Jest passed (`7/7`), focused lint passed, and frontend typecheck passed.
- 2026-06-23: Shared frontend diary viewer coverage update. The frontend added direct tests for `EntryViewerPortal`, `ViewFullEntrySideSheet`, and `ViewReachEditor`, covering portal forwarding, content-ready pending open/close timeout lifecycle, and read-only rich-editor expanded/collapsed HTML behavior. Focused frontend viewer Jest passed (`15/15` with existing integration), focused lint passed, and frontend typecheck passed.
- 2026-06-23: Shared frontend diary edit-wrapper coverage update. The frontend added direct `EditEntryTitle` / `EditEntryContent` tests, fixed edit-title fallback so an empty `entryTitleRef.current` uses the edit draft title before effect sync, removed dead title styles, and memoized/stabilized the edit content wrapper root/scroll styles. Focused frontend edit-wrapper Jest passed (`32/32` with related create/view tests), focused lint passed, and frontend typecheck passed.
- 2026-06-23: Shared frontend calendar small-primitives coverage update. The frontend added direct `CalendarMonthYearPicker` and `ClockFace` tests; the picker now clears pending delayed apply timeouts on close/unmount/before reapply so hidden pickers cannot fire late month changes. Focused frontend calendar Jest passed (`31/31` with Week/Month/summary chart), focused lint passed, and frontend typecheck passed.
- 2026-06-23: Shared frontend optimization focus moved from Diary to Progress. User considers Diary sufficiently optimized/test-covered for now and wants the next pass to start at `app/(tabs)/progress.tsx`, then proceed through `components/progress/*`, with the same rule: no app-file edits until explicit "внеси зміни"; after edits update/add tests and run focused lint/Jest/typecheck before reporting completion.
- 2026-06-23: Shared frontend Calendar perf note before Progress: user suspects calendar switching/gorting may have slowed. Initial audit suggests the latest small `ClockFace`/`CalendarMonthYearPicker` testID/timeout cleanup batch is not the direct cause; more likely suspects are earlier UI-delay changes in `MonthView` (40ms delayed month apply and 180ms nav lock/loader) and `WeekView` week-to-month `changeCalendarLoader` + `requestAnimationFrame`. Preserve no-card-remount/flicker rule when fixing.
- 2026-06-23: Shared frontend Calendar perf experiment result: removing the `MonthView` month-nav delay/lock was tried and rolled back because user confirmed it did not make switching faster. Do not repeat that as the next primary fix. After rollback, `MonthView` runtime behavior is back to the delayed apply/180ms lock path; only `tests/unit/components/diary/MonthView.test.js` kept lint metadata/unused-variable cleanup. Next candidate should inspect day-entry loading/card readiness or week/month loader flow.
- 2026-06-24: Shared frontend navigation rollback before Progress optimization. Calendar is now the main/home tab and app index redirects to `/(tabs)/calendar`; the old Diary feed route file remains in the repo as legacy code but is removed from visible tabs/configurable tab order, so it should not render from tab navigation. Calendar uses the former Diary book tab icon. Default visible tab order is `calendar`, `progress`, `goals`, `community`, `settings`; old saved diary-first default order migrates to this new default, and custom orders normalize by dropping `diary`. Create-entry and check-in create/detail fallback flows return to `/calendar`. Focused frontend Jest/lint/typecheck passed.
- 2026-06-24: Shared frontend Calendar post-create regression fixed. After an entry is saved, Calendar listens for `EntryEvents.entryCreated`, switches selected/anchor/initial date back to today, shows week view, clears the calendar-change loader, and refreshes today's entries plus moods/emoji, week metric summary, and plan. Focused frontend Calendar Jest/lint/typecheck passed.
- 2026-06-24: Shared frontend empty-state UX note. Calendar and active goal empty states returned to the old large first-action composition/text/button, but with the newer lightweight arrow and new calendar/goal icons. Empty-state icons should read as background markers, not primary accents: use soft theme-token glow (`secondaryAccentTop/Mid`) and avoid strong backplates. Goal empty-state icon sizes are aligned across active and secondary tabs (`deadline` 46, `checklist` 32, `habit` 40) with the new `colors.secondary` icon color.
- 2026-06-24: Shared frontend segmented-control note. `SegmentedControl` default layout now stretches segments from the first render instead of waiting for layout/content measurement, fixing the transient unselected/jitter look in goal tab switches. Scroll overflow behavior is explicit via `scrollable`. The control is visually lifted with theme surface shadow, small elevation/shadow, visible overflow, and `SurfaceOverlay` outer glow/depth `0.42`; goal tab wrappers memoize options/change handlers.
- 2026-06-24: Shared frontend segmented-control lift correction. Bottom shadow/elevation alone did not read as floating, so `SegmentedControl` was restructured into an outer wrapper, floating shell, inner capsule, and scroll/content layer. The intended lift cue is full-perimeter and theme-token based: halo from `surface.base.outerGlowSoft`, top rim from `surface.base.topLightSoft`, and only subtle capsule shadow, not a strong bottom-only shadow.
- 2026-06-24: Shared frontend floating surface refinement. For goal secondary tabs, the goal-type select, and weekly calendar, the accepted visual pattern is a separate floating/shadow wrapper outside the clipped surface, matching cards and the bottom tab bar. Use theme `shadowSoft`, visible overflow, and stronger elevation on the wrapper; keep the `SurfaceOverlay`/glass content clipped inside. Goal secondary tabs sit slightly higher on the goals screen (`marginTop: -8`); the weekly calendar has a `calendar-week-floating-shell`; the goals CustomSelect trigger keeps measurement/tooltip anchoring on the inner anchor view.
- 2026-06-24: Shared frontend calendar visual correction. Weekly calendar should now match the monthly calendar card treatment, not the retired shaped/SVG surface: rounded clipped card (`borderRadius: 20`) with MonthView-style shadow geometry and `elevation 8` after manual visual review, and no separate rectangular colored/shadowed backplate behind it.
- 2026-06-24: Shared frontend weekly-calendar affordance note. The week-to-month chevron can sit outside the card as a floating handle, but it should remain tiny and background-like; a large white pill version was rejected because it drew too much attention.
- 2026-06-24: Shared frontend calendar chart UX requirement. Calendar metric summary should reserve/render its chart slot from first render; when there is no metric data, show a small in-chart empty text (for example "Немає даних") instead of unmounting/returning `null`, so the weekly calendar does not visually jump when metric data arrives.
- 2026-06-24: Shared frontend calendar chart implementation completed. Weekly calendar now always reserves chart height, `CalendarMetricSummaryChart` always renders the grid/slot, empty state uses localized `calendar.metricSummaryNoData`, and arriving data fades in over 2400ms through opacity instead of popping in.
- 2026-06-24: Shared frontend form-label UX. Label backplates for shared form controls should be content-width only, not full-width. Applies to text input, password input, text area, select, multi-select, and checkbox labels via an outer alignment wrapper plus an inner non-stretched `SettingsTitleSurface`.
- 2026-06-24: Shared frontend weekly-calendar dark-theme contrast note. The small week-to-month expand chevron stays subtle but should use `colors.textSecondary` on dark-background themes (`colors.barStyle === "light"`) because `colors.icon` can be too low-contrast there.
- 2026-06-24: Shared frontend `SettingsTitleSurface` note. Visual experiments with chip/surface, BlurView, line-height alpha fades, and fully transparent backing were rejected/rolled back. The component keeps the original visible torn/light empty-state veil backing with original `emptyStateVeil*` colors/vertical locations, plus a `MaskedView` horizontal side fade so left/right edges are also soft/torn instead of rectangular.

- 2026-06-24: Shared frontend design note: user accepted the current `SettingsTitleSurface` side-faded torn backing as good enough for now; later frontend work may change background images, tune `primary`, and split these label/backing tokens into a separate theme-token group.
- 2026-06-24: Shared frontend Progress optimization checkpoint: first scoped `app/(tabs)/progress.tsx` batch completed. The frontend consolidated chart width state, stabilized Progress period/custom-date/layout handlers, moved repeated page inline styles to `StyleSheet`, and added Progress integration coverage for `entryUpdated` reload/listener cleanup. Focused frontend Progress lint/Jest/typecheck pass.
- 2026-06-24: Shared frontend `SegmentedControl` follow-up: fixed/non-scroll Progress/Goals tabs now shrink inside the capsule and active labels auto-fit font size, addressing clipping on the active tab with bolder/larger text. Focused frontend SegmentedControl lint/Jest/typecheck pass.
- 2026-06-24: Shared frontend Progress chart checkpoint: `MetricDotChart` now memoizes defined point geometry/colors and reuses it for paths, dots, badge, and hit targets; repeated static styles moved to `StyleSheet`, dead SVG imports/helper removed, and focused frontend chart lint/Jest/typecheck pass.
- 2026-06-24: Shared frontend Progress wrapper checkpoint: all five metric wrapper charts are memoized, their stats-to-series mapping is memoized, and icon label render callbacks are stable while preserving the existing titles/icons/color scales/metric fields. Focused frontend wrapper lint/Jest/typecheck pass.
- 2026-06-24: Shared frontend Progress icon checkpoint: five metric SVG icons now hoist static paths/constants, remove dead imports/helpers, avoid per-render static array/object creation, and have direct focused icon coverage for guard/size/color/shape/memo behavior. Focused frontend icon lint/Jest/typecheck pass.
- 2026-06-24: Shared frontend Progress component sweep completed. `components/progress` is clean after full focused lint, component Jest (`28/28`), Progress screen Jest (`5/5`), and frontend typecheck; palette module was reviewed and no debug/TODO suppressions were found. No extra code churn was needed in the sweep.
- 2026-06-24: Shared frontend goals design checkpoint: create/edit goal screen now uses a lifted "goal identity" card for color/emoji/type plus shared lifted form sections and card-like milestone rows. The frontend preserved existing save/validation/reorder/date/type behavior and verified with focused goal form Jest, app-code ESLint, and typecheck. Future goal-card/list redesign should align with this floating-card surface language.
- 2026-06-24: Shared frontend goals/check-ins design correction: user does not want the same rounded lifted card/glass language repeated everywhere. Goal and check-in creation flows should get a distinct creation-specific visual language, potentially backed by new theme tokens/color accents, rather than reusing the generic card/settings surface style.
- 2026-06-24: Shared frontend goals creation canvas experiment: implemented only for goals using existing theme tokens (`veilSecondary`, `secondaryAccent`, existing surface/primary fallbacks), without adding new token keys yet. The create/edit goal header is now a lighter goal-passport/control area, form sections are non-elevated creation bands with an accent rail, and milestones read as route/timeline steps. Check-ins are intentionally unchanged for now.
- 2026-06-24: Shared frontend goals creation refinement: the frontend now reuses the habit weekday unselected readability material (`SurfaceOverlay` on `colors.surface.veil`, soft/depth `0.2`) behind small goal-creation controls: color/emoji controls, milestone numbers/action icons, and the add-another-milestone pill. Color/emoji labels are vertically aligned via `space-between`, and milestone action icons remain separate tap targets.
- 2026-06-24: Shared frontend goals deadline polish: keep create/edit goal background blur at `2`. The deadline add-milestones checkbox uses local `Checkbox` alignment overrides so checkbox and text align vertically without changing default checkbox layout elsewhere.
- 2026-06-24: Shared frontend goals deadline milestone action polish: deadline milestone reorder/delete icons now sit on the right edge of the milestone body, with each icon in its own small `secondaryAccent`/readability-overlay container. Checklist milestone actions are intentionally unchanged for now.
- 2026-06-24: Shared frontend goals creation text readability rule clarified: text labels/headings keep the standard `SettingsTitleSurface` preset without local veil/content overrides; `GoalReadabilityOverlay` stays for controls/icons. Date labels, add-milestones labels, and milestone section titles now use matching text backing behavior. Checklist milestone actions now match deadline actions: right-aligned and individually backed by small `secondaryAccent`/readability-overlay containers.
- 2026-06-24: Shared frontend structured check-in create/edit design started moving away from the generic large rounded card. `StructuredCheckinScreen` and `StructuredCheckinDetailsScreen` now render questions as a vertical flow/timeline with a thin accent rail and standard `SettingsTitleSurface` labels, while preserving date/state/save/AI/check-in logic. The local `tuneAlpha` helper is defensive for missing theme tokens, and rail colors use real `secondaryAccentTop/Mid` tokens with fallbacks. Focused frontend check-in Jest, app-file ESLint, and typecheck pass.
- 2026-06-24: Shared frontend structured check-in timeline/rail experiment was rejected visually: dots/rail and per-label torn surfaces read as random spots and still hurt readability over photo backgrounds. Current direction: no rail and no per-question `SettingsTitleSurface`; use one soft horizontal reading veil behind the whole question stack, stronger question text, and slightly more opaque answer fields while avoiding a generic lifted card.
- 2026-06-24: Shared frontend structured check-in single horizontal reading veil was also rejected visually: it reads as a rectangular curtain over the photo background and still does not feel right. Next check-in form attempt should avoid a full-block veil; prefer either a calmer/dimmed creation background for the form area or individual matte input surfaces with label/readability handled more subtly.
- 2026-06-24: Shared frontend new structured check-in design direction to try next: treat each question + answer input as one separate transparent, volumetric surface card, not the old large generic rounded card and not separate label/input layers. Use existing `SurfaceOverlay`/theme surface tokens, likely a subtle `glass` or `soft` overlay with low depth, per-question radius, transparent volume, and internal label/input hierarchy.
- 2026-06-24: Shared frontend structured check-in per-question surface experiment implemented. Create/details forms now wrap each question + answer field in its own low-depth transparent `SurfaceOverlay` glass card (`colors.surface.veil`, radius 24), remove the separate label `SettingsTitleSurface`, and make the inner TextArea field more subtle/transparent so the card carries readability. Focused frontend app-file ESLint, check-in Jest, and typecheck pass.
- 2026-06-24: Shared frontend structured check-in per-question surface experiment was rejected visually as still too card-like/heavy. First replacement variant implemented for create/details: remove the per-question `SurfaceOverlay` cards, restore standard `SettingsTitleSurface` only under question labels, and make answer fields transparent writing-line surfaces with a subtle bottom rule. Focused frontend app-file ESLint, check-in Jest, and typecheck pass.
- 2026-06-24: Shared frontend structured check-in first replacement variant corrected to match the intended notebook/paper idea. `TextArea` now has an opt-in `ruledLines` mode; structured check-in create/details answer fields use it with light theme-token horizontal rules and a subtle paper-like input surface, instead of just a single bottom line. Focused frontend app-file ESLint, check-in Jest, and typecheck pass.
- 2026-06-24: Shared frontend `TextArea` ruled-paper mode is covered by `ThemedTextInput.test.js`: by default no ruled lines render, and `ruledLines` renders themeable horizontal writing lines. Focused TextArea/check-in Jest (`24/24`), focused ESLint, and frontend typecheck pass.
- 2026-06-24: Shared frontend structured check-in ruled-paper fields were strengthened after visual review: the previous translucent paper dissolved into the background and lines were too faint. Create/details answer fields now use a near-opaque theme input background, visible theme-token ruled lines, a light hairline border, and small radius so they read more like paper while still avoiding the old large card wrapper. Focused TextArea/check-in Jest (`24/24`), focused ESLint, and frontend typecheck pass.
- 2026-06-24: Shared frontend next structured check-in paper-field refinement requested from screenshot review: remove border radius from the paper input, make top and bottom edges look torn/irregular rather than rounded, and align horizontal ruled lines with the text baseline/line-height so typed text sits naturally on the lines.
- 2026-06-24: Shared frontend structured check-in ruled-paper refinement implemented. `TextArea` paper mode now supports themeable paper edge color and ruled-line offset, renders opt-in torn top/bottom edge pieces, and stays disabled for normal textareas. Create/details structured check-in answer fields now use square paper surfaces with only side hairlines, no border radius, a lower first rule, and 24px line-height so typed text sits more naturally on the rules. Focused frontend TextArea/check-in Jest (`24/24`), focused ESLint, and typecheck pass.
- 2026-06-24: Shared frontend structured check-in ruled-paper line alignment adjusted after device screenshot. Create/details answer inputs now use Android-friendly text rhythm: `lineHeight: 28`, `includeFontPadding: false`, `paddingTop: 6`, and `ruledLineOffset: 14`, so the absolute ruled lines should track multiline text rows instead of drifting by later rows. Focused frontend TextArea/check-in Jest (`24/24`), focused ESLint, and typecheck pass.
- 2026-06-24: Shared frontend structured check-in ruled-paper spacing widened after follow-up screenshot. Current create/details test geometry is `lineHeight: 32`, `includeFontPadding: false`, `paddingTop: 4`, and `ruledLineOffset: 6`: wider row spacing while lifting the first rule compared with the previous too-low offset. Focused frontend TextArea/check-in Jest (`24/24`), focused ESLint, and typecheck pass.
- 2026-06-24: Shared frontend structured check-in ruled-paper model corrected after failed line-height attempts. Text rhythm and paper rule spacing are now separate: answer text is back to `lineHeight: 24`, while `TextArea` accepts `ruledLineStep` for absolute rule spacing and counts ruled lines from the input row count (`height / lineHeight`), not from the rule spacing. Current check-in props use `ruledLineOffset: 4` and `ruledLineStep: 32`, so these can be tuned without changing text line-height. Focused frontend TextArea/check-in Jest (`24/24`), focused ESLint, and typecheck pass.
- 2026-06-24: Shared frontend structured check-in paper scroll behavior fixed. `TextArea` now flattens `inputStyle` before reading `lineHeight`, which matters for details/edit where input styles are arrays. In `ruledLines` mode it ignores `maxLines/maxHeight`, keeps `scrollEnabled={false}`, and grows to full content height so the outer screen scrolls and the paper lines move with the text instead of staying fixed inside a scrolling native `TextInput`. Focused frontend TextArea/check-in Jest (`25/25`), focused ESLint, and typecheck pass.
- 2026-06-24: Shared frontend structured check-in torn-paper edge experiment applied as a small reversible change. Only the `TextArea` paper edge pattern changed: top and bottom now use separate irregular segment arrays with varied widths/heights, while TextInput growth, line clipping, rule spacing, and scroll behavior stay unchanged. If the visual does not work on device, rollback is just restoring the previous single `PAPER_EDGE_SEGMENTS` pattern. Focused frontend TextArea/check-in Jest (`25/25`), focused ESLint, and typecheck pass.
- 2026-06-24: Shared frontend structured check-in paper contrast adjusted for dark themes. Create/details paper fields now use `colors.backgroundSecondary` when `colors.barStyle === "light"` (dark themes) and keep the previous tuned `inputBackground` paper color on light themes; the same resolved color is passed to `paperEdgeColor` so the torn edge matches the paper body. Existing rule spacing/offset values were preserved. Focused frontend TextArea/check-in Jest (`25/25`), focused ESLint, and typecheck pass.
- 2026-06-24: Shared frontend goals card redesign direction started. The three goal card variants (deadline/checklist/habit) should be redesigned beyond minor polish: preserve behavior, rethink card layout/information hierarchy, and allow either keeping the existing glass/card surface or replacing/reworking it if that gives a stronger result.
- 2026-06-24: Shared frontend goals card redesign first batch implemented. Frontend added shared `GoalPassportCard` primitives and moved deadline/checklist/habit cards toward a passport layout: compact meta/menu top, emoji/title identity, type-specific footer preview, and separated expanded action area. Deadline/checklist milestones are lighter route/step rows; habit weekday rhythm uses compact chips. Stats loaders were stabilized, duplicate deadline stats loading removed, and deadline delete stats origin corrected. Focused goal-card ESLint/typecheck/Jest pass.
- 2026-06-24: Shared frontend goals card redesign correction after screenshots: the first Goal Passport batch was not visually accepted because it mostly wrapped the old layout instead of truly changing composition and hierarchy. Future goal-card work should be a real layout rewrite of collapsed/expanded states and type-specific previews/actions, not another shared-component extraction.
- 2026-06-24: Shared frontend goals card redesign second pass implemented a real layout rewrite: habit/checklist/deadline cards now use a compact passport identity row with percent status, stat chips, and type-specific preview panels; expanded habit has a separate check-in action dock, while checklist/deadline milestones read more like lightweight route rows. Focused frontend goal-card Jest, ESLint, and typecheck passed.
- 2026-06-24: Shared frontend habit-card note: the small follow-up experiment to enlarge the habit title, move percent beside the progress scale, and replace text stat chips with icon-only metric chips was rejected visually and rolled back. Treat the current second-pass Goal Passport habit card as the baseline unless the next attempt is a fuller card composition rewrite.
- 2026-06-24: Shared frontend habit-card follow-up: a constrained revision is now implemented for visual review. Only the habit title is +2 (`20/24`), the existing percent status card is reused unchanged but moved below the identity row to the right of the status/progress cluster, and status pills use icon + number instead of words. Focused frontend goal-card Jest, ESLint, and typecheck pass.
- 2026-06-24: Shared frontend habit status pill icon semantics refined after visual review. Done stays `checkmark`, missed stays `close`, partial should read as a half/partial state (`contrast` in the current Ionicons set), and at-most exceeded should read as "over limit" (`trending-up`) rather than a generic warning/exclamation.
- 2026-06-24: Shared frontend goals card color treatment experiment: `GoalsCardWrapper` now accepts `accentColor` and renders each goal's color as a subtle 6px left-side strip inside the rounded card. Habit/Checklist/Deadline cards pass their `goalColor`, preserving behavior while making color a stronger card identity cue.
- 2026-06-24: Shared frontend goals card percent badge treatment refined: `GoalPassportStatus` uses percent-based status tones (>=75 green, 25-74 stronger yellow, <25 red) rather than the goal identity color, with neutral text for readability. The header color dot was removed because the left accent strip already carries goal color identity.
- 2026-06-24: Shared frontend Checklist/Deadline card parity with Habit: percent badges sit beside the status pills/progress bar instead of inside the identity row, and text stat chips were replaced with shared icon+number `GoalPassportStatPill` pills. Habit was switched to that shared pill component as well. Checklist/Deadline briefly used a stronger `22/27` title override, but after visual review all three goal card types should use the same `20/24` title size.
- 2026-06-24: Shared frontend habit-card premium-polish experiment applied first only to Habit before copying to Checklist/Deadline. Passport primitives now have optional compact identity, softer emoji badge, quieter preview, and configurable card accent strip. Habit uses a thinner/less opaque left accent, quieter emoji badge, tighter identity/progress/preview spacing, and a lighter preview pill to reduce empty air while preserving the accepted layout.
- 2026-06-24: Shared frontend goals segmented switch refined with a dedicated `SegmentedControl` `variant="goals"` so Progress keeps the default style. Habit/Checklist/Deadline tabs now use the quieter/lower switch variant with reduced shadow/sheen, smaller `xl` label mapping, and tighter margins, keeping the switch below the new goal cards in visual hierarchy.
- 2026-06-24: Shared frontend habit-card premium polish was fully copied to Checklist/Deadline, including the same `20/24` card title size. Checklist/Deadline now match Habit on thinner/quieter left accent, compact header, soft identity icon, tighter progress cluster, lowered status pills, quiet preview surface, and muted chart segment colors.
- 2026-06-24: Shared frontend goals type selector note: recent light/secondary and `primary`+transparent-surface capsule experiments were rejected on device. Future frontend work should not keep token-swapping the same capsule; either return closer to the old darker select or rethink placement/interaction.
- 2026-06-24: Shared frontend goals type selector was returned to the accent surface for now: the goal-specific `CustomSelect` quiet variant keeps compact/larger-label sizing, but uses `colors.surface.accent` backing/overlay again.
- 2026-06-25: Shared frontend goals card danger signaling refined. Deadline overdue state should show on collapsed cards through semantic red (`colors.error`) in chart/percent/status colors, without a standalone collapsed "overdue" label. The overdue label belongs only to the specific overdue milestone in expanded deadline cards, above the milestone title. Habit red chart/status states also use `colors.error`, and the tiny unreadable today-count badge over the habit action/menu icon was removed. Focused frontend goal-card Jest, ESLint, and typecheck pass.
- 2026-07-13: Backend diary-tab experiment added. `user_settings` now stores immutable `diaryTabVariant` (`legacy`, `diary_and_calendar`, `calendar_only`) and mutable `diaryTabEnabled`. Existing rows receive entity-column defaults (`legacy` + enabled) through the project's startup schema synchronization; no migration file is used. Assignment is off by default and requires both `DIARY_TAB_EXPERIMENT_ENABLED=true` and a valid positive `DIARY_TAB_EXPERIMENT_MIN_BUILD`. Eligible new users are assigned deterministically by saved numeric user id parity (even: diary+calendar, odd: calendar only); older builds and disabled/invalid config stay `legacy` + enabled. Client settings updates may change `diaryTabEnabled` but cannot overwrite the original variant. Users created below the minimum build remain `legacy` after an app upgrade and are not retroactively assigned to the experiment. Settings already store `appVersion`/`appBuild`; login and `/users/me` return the full settings object. Verification: typecheck passed; experiment helper tests 6/6 and focused UsersService tests 2/2 passed.
- 2026-07-15: Retention cleanup added. `LogsCronService` deletes `logs` and `server_http_logs` rows older than 30 days every day at 03:00 Europe/Kyiv. `AiResponseMonitoringCronService` deletes `ai_response_monitoring_records` older than 7 days every day at 03:15 Europe/Kyiv. Cleanup queries use indexed `createdAt`; repository failures are isolated so one log table does not block cleanup of the other. AI monitoring remains controlled by its global switch and sampling settings, so policy wording must describe it as selective, temporary storage rather than continuous collection.
- 2026-07-15: Full backend verification completed after retention cleanup. Unit Jest is green across all 23 suites / 188 tests (21 suites / 185 tests from the full run plus 2 cron suites / 3 tests rerun after fixing explicit Jest imports and typed mocks); typecheck and production build pass. Repository-wide lint remains red with 3744 existing issues (3477 errors / 267 warnings, largely CRLF Prettier and `no-unsafe-*` debt). Integration is blocked by missing Google Application Default Credentials because tests call the real Google Play API (2 suites / 3 tests fail). E2E has 4/9 suites and 14/25 tests passing; failures are stale test fixtures missing the `IapController` `SubscriptionsService` provider and the `ForumTopicReadStatesService.markAllExistingTopicsAsReadForNewUser` mock.
