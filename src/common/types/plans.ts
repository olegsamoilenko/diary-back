export enum Plans {
  START = 'start',
  LITE = 'lite',
  BASE = 'base',
  PRO = 'pro',
}

export enum PlanCost {
  START = 0,
  LITE = 10,
  BASE = 20,
  PRO = 40,
}

export enum PlanTokens {
  START = 360000,
  LITE = 850000,
  BASE = 1700000,
  PRO = 3400000,
}
